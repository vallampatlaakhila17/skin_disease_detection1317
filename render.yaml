services:
- type: web
  name: flask-ai-grader
  env: python
  buildCommand: "pip install -r requirements.txt"
  startCommand: "gunicorn --bind 0.0.0.0:$PORT api.app:app"
  envVars:
  - key: DATABASE_URL
    sync: false
  - key: FLASK_SECRET_KEY
    sync: false
